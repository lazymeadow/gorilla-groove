import Foundation
import os

let GGLog = Logger(subsystem: Bundle.main.bundleIdentifier!, category: "standard")
let GGNavLog = Logger(subsystem: Bundle.main.bundleIdentifier!, category: "navigation")
