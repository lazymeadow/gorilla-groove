<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:ignore="RtlSymmetry">

    <TextView
        android:id="@+id/switchControlText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:background="@color/grey1"
        android:padding="10dp"
        android:paddingStart="12dp"
        android:textAlignment="textStart"
        android:textColor="@color/foreground"
        android:textSize="17sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Enabled" />

    <com.google.android.material.switchmaterial.SwitchMaterial
        android:id="@+id/switchControlSwitch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:checked="true"
        app:layout_constraintBottom_toBottomOf="@id/switchControlText"
        app:layout_constraintEnd_toEndOf="@id/switchControlText"
        app:layout_constraintTop_toTopOf="@id/switchControlText" />

</androidx.constraintlayout.widget.ConstraintLayout>
