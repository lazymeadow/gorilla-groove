export const TrackView = Object.freeze({
	LIBRARY: 'library',
	USER: 'user',
	PLAYLIST: 'playlist',
	NOW_PLAYING: 'nowPlaying'
});

export const CenterView = Object.freeze({
	TRACKS: 0,
	REMOTE_DEVICES: 1,
	GLOBAL_SEARCH: 2
});
